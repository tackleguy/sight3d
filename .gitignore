node_modules/
dist/
test-results/
# OBJ 3D model files (not the file.obj/ implementation directory)
*.obj
!implementations/file.obj/
*.sketch
.DS_Store
settings.yaml
release/
.env.signing

# Never commit .skp models (redistribution risk)
*.skp

# Local UI verification captures
.impeccable/review/
